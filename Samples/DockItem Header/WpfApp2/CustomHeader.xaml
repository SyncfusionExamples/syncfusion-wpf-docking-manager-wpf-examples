<UserControl x:Class="WpfApp2.CustomHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp2"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVisibilityConverter" />
    </UserControl.Resources>
    <StackPanel x:Name="stackPanel" Orientation="Horizontal">
        <StackPanel Orientation="Horizontal">
            <Image Width="16" Height="16" Margin="3,0,0,0">
                <Image.Style>
                    <Style TargetType="Image">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DocumentTitle}" Value="ToolBox">
                                <Setter Property="Source" Value="/Toolbox-02.png"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding DocumentTitle}" Value="Admin">
                                <Setter Property="Source" Value="/Admin2.png"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding DocumentTitle}" Value="Docking">
                                <Setter Property="Source" Value="/Docking-Manager.png"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding DocumentTitle}" Value="Clipboard">
                                <Setter Property="Source" Value="/Clipboard-02.png"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding DocumentTitle}" Value="Bookmark">
                                <Setter Property="Source" Value="/Bookmark Delete01-WF.png"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>
            <TextBlock
      Margin="3,0"
      VerticalAlignment="Center"
      Text="{Binding DocumentTitle}" >
            </TextBlock>
        </StackPanel>
    </StackPanel>
</UserControl>
